parallel quicksort

lock array

is a[i] >or< pivot?
in parallel, compare them

each thread get lock, put on relevant side of pivot

-------------------------

array -> array1 || array2

parallelsort(){
size
array
compare(array, pivot)
pthread(parallelsort(array[0:pivot-1]))
pthread(parallelsort(array[pivot+1:size]))

}

compare(){
//move larger right and smaller left

}
